version: 0.2

phases:
  install:
    commands:
      - echo Installing AWS CLI...
  
  build:
    commands:
      - echo Build started
      - echo Uploading files to S3
      - aws s3 sync . s3://shankar-bucket-21-4

artifacts:
  files:
    - '**/*'
